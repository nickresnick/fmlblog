<p id="notice"><%= notice %></p>

<%= form_for posts_path, :method => 'get' do %>
    <p>
      <%= collection_select(:search, :topic_id, Topic.all, :id, :name, prompt: "All Topics", :input_html => {:size => 40})   %>
      <%= collection_select(:search, :user_id, User.all, :id, :name, prompt: "All Users", :input_html => {:size => 40})   %>
      <%= submit_tag "Search", :name => nil, :style => 'width: 100px' %>
    </p>
<% end %>

<%= render 'shared/feed' %>
