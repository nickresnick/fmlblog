<p id="notice"><%= notice %></p>

  <tbody>
    <% @topics.each do |topic| %>
        <td><%= topic.name %></td>
        <% topic.posts.each do |post| %>
        <td><%= post.content %></td>
            <% end %>
    <% end %>
  </tbody>

<br>

<%= link_to 'New Topic', new_topic_path %>
