<div class="container-full">
  <div class="container special">
    <div class="jumbotron">
      <h1>Film & Music & Life Blog</h1>

      <h2> Everything from film criticism to life criticism. Guest posts from the roommates. <br>
        Check out the archive <%= link_to "here", posts_path %>.
      </h2>
    </div>
  </div>
</div>

<div class="row">
</div>
<div class="row">
  <div class="col-md-6">
      <%= render 'shared/feed' %>
  </div>

    <aside class="col-md-6">
    <h3>Most Recent Posts</h3>
    <%= @posts.each do |post| %>
          <><%= link_to post.title, post %>     <%= post.created_at.to_date %></p>
    <% end %>
  </aside>
  </div>
<div>
  <% if logged_in? %>
    <%= link_to 'New Post', new_post_path %>
  <% end %>
</div>



