<table>
<tbody>
<% @posts.each do |post| %>
<div class="row">
  <div class="col-md-6">
        <h3><%= link_to post.title, post %></h3>
    <p>by <%= link_to post.user.name, post.user %> on
      <%= post.created_at.in_time_zone.strftime('%m/%d/%y')%> under
      <%= link_to post.topic.name, post.topic %></p>
        <p>
          <%=  truncate(post.content, :length => 50, :escape => false).html_safe %>
        </p>
    <h5><%= link_to pluralize(post.comments.count, 'Comment'), posts_path(anchor: "comments")%></h5>
    <h5><%= pluralize(post.impressionist_count, 'View') %></h5>
  </div>
  <div class="col-md-6">
        <h2><%= image_tag post.picture.url, :height => "65%", :width => "65%" %></h2>
  </div>
</div>
    <hr>
<% end %>
</tbody>
</table>
